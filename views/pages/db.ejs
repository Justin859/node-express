<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.provider %> -- <%= r.name %> -- <%= r.id %> <%= r.created_at %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
